<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:innerRadius="80dp" android:shape="ring" android:thickness="2dp" android:useLevel="false">
            <solid android:color="@color/colorwhite1" />
        </shape>
    </item>
    <item>
        <shape android:innerRadius="100dp" android:shape="ring" android:thickness="2dp" android:useLevel="false">
            <solid android:color="@color/colorwhite1" />
        </shape>
    </item>
    <item>
        <shape android:innerRadius="120dp" android:shape="ring" android:thickness="2dp" android:useLevel="false">
            <solid android:color="@color/colorwhite1" />
        </shape>
    </item>
</layer-list>